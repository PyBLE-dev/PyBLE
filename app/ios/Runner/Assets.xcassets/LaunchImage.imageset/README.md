# Launch Screen Assets

The 1x, 2x, and 3x launch images are release assets derived from
`app/assets/branding/pyble-prompt-chip-master.png`. They are centred on the
matching `#081B35` launch-screen background so the mark remains stable across
iPhone and iPad aspect ratios.
