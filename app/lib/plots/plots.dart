// SPDX-License-Identifier: MIT
// Part of PyBLE (https://pyble.dev) — see /LICENSE.

/// PyBLE `lib/plots/` — `fl_chart` views derived purely from console output
/// (CSV / streamed values) with configurable parsing
/// (see docs/specifications/app.md §2). No data-event opcode; neutral types
/// only. Binds only to the `Connection` seam (CON-8).
///
/// S1 skeleton placeholder barrel. Real content lands at story A-32.
library;
